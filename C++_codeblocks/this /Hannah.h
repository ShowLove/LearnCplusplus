#ifndef HANNAH_H
#define HANNAH_H


class Hannah
{
    public:
/*whenever we create an object from Hannah we are
going to pass it in an integer value. if we pass in 20
its gonna set h = 20... were doing that with the constructor*/
        Hannah(int);
       void printCrap();

    private:

    int h;
};

#endif // HANNAH_H
